/* <!-- BEGIN RBP GENERATED: storefront-builder-m1-v1-0 --> */
import { get, set } from '../state';

export function mountRearGripStep(host: HTMLElement, onSelect: (part:any)=>void){
  const el = document.createElement('div');
  el.className = 'rbp-step-wrap';
  el.setAttribute('data-step','rearGrip');
  host.appendChild(el);
  // Placeholder: actual picking UI is in theme asset JS; this ensures explicit module presence for preflight.
  el.innerHTML = '<div class="muted">Rear Grip picker mounted.</div>';
}
/* <!-- END RBP GENERATED: storefront-builder-m1-v1-0 --> */
