{
  "name": "rbp-gitrepo-v1-3",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
  "dev:shopify": "shopify app dev --path=src/apps/rbp-shopify-app/rod-builder-pro --store=rbp-dev.myshopify.com",
  "dev:shopify:reset": "shopify app dev --reset --path=src/apps/rbp-shopify-app/rod-builder-pro --store=rbp-dev.myshopify.com",
  "deploy:shopify": "cd src/apps/rbp-shopify-app/rod-builder-pro && pnpm shopify app deploy",
  "smoke": "node ./scripts/smoke.mjs",
  "test": "echo \"Error: no test specified\" && exit 1",
    "curl:checks": "bash scripts/curl-checks.sh",
    "curl:builds": "bash scripts/curl-builds.sh",
    "curl:builds:local": "source scripts/port.env && bash scripts/curl-builds.sh",
  "curl:checks:local": "source scripts/port.env && bash scripts/curl-checks.sh"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.14.0",
  "devDependencies": {
    "prisma": "^6.14.0"
  }
}